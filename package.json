{
  "name": "tone-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Brand tone-of-voice drop announcement dashboard. Reads a hospitality brand's website, Instagram and press coverage, then drafts SMS / Email / Instagram announcements in that brand's voice.",
  "type": "module",
  "scripts": {
    "bump": "node scripts/bump-version.js",
    "dev": "npm run bump && node server.js",
    "vercel-dev": "npm run bump && vercel dev",
    "deploy": "npm run bump && vercel --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.87.0",
    "@google/genai": "^1.50.1",
    "puppeteer": "^24.41.0"
  },
  "engines": {
    "node": ">=20"
  }
}
